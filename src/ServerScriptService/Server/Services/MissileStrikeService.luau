--!strict
--[[
	v69 Command Center missile strikes on other bases' defenses. Stub: the real service replaces this file.
	Remotes: RequestMissileStrike (client → server), MissileStrikeStateUpdate (server → client),
	MissileStrikeFx (server → all clients, visuals).
]]

local MissileStrikeService = {}

function MissileStrikeService.Init(_deps: { [string]: any })
	print("[WAR EMPIRE] MissileStrikeService stub (no strikes yet)")
end

return MissileStrikeService
